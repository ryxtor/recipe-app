<% @inventories.each do |inventory| %>
  <div>
    <%= link_to inventory.name, inventory %>
    <%= inventory.description %>
    <% if can? :manage, inventory %>
    <%= button_to 'Remove', inventory_path(inventory), method: :delete %>
    <% end %>
  </div>
<% end %>